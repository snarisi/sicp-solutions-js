/*Exercise 1.9.  Each of the following two procedures defines a method for adding two positive integers in terms of the procedures inc, which increments its argument by 1, and dec, which decrements its argument by 1.*/

//first define inc and dec to use for testing
function inc(a) {
    return a + 1;
}
function dec(a) {
    return a - 1;
}

//then the two plus functions
function plus(a, b) {
    if (a === 0) {
        return b;
    } else {
        return inc(plus(dec(a), b));
    }
}


function plus(a, b) {
    if (a === 0) {
        return b;
    } else {
        return plus(dec(a), inc(b));
    }
}



/*Using the substitution model, illustrate the process generated by each procedure in evaluating (+ 4 5). Are these processes iterative or recursive?*/

/*
First function - recursive:

plus(4, 5);

inc(plus(dec(4), 5));
inc(plus(3, 5));
inc(inc(plus(dec(3), 5)));
inc(inc(plus(2, 5)));
inc(inc(inc(plus(dec(2), 5))));
inc(inc(inc(plus(1, 5)));
inc(inc(inc(inc(plus(dec(1), 5)))));
inc(inc(inc(inc(plus(0, 5)))));
inc(inc(inc(inc(5))));
inc(inc(inc(6)));
inc(inc(7));
inc(8);
9;
*/
 


/*
Second function - iterative:

plus(4, 5);

plus(dec(4), inc(5));
plus(3, 6);
plus(dec(3), inc(6));
plus(2, 7);
plus(dec(2), inc(7));
plus(1, 8);
plus(dec(1), inc(8));
plus(0, 9);
9;
*/
